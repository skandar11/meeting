# transit — тамбур

Сюда `engine/snapshot.py` кладёт снимки WhatsApp: `whatsapp-ГГГГ-ММ-ДД-ЧЧММ/`.

Снимок — копия всей переписки WhatsApp на этом Маке, поэтому:

- **закрыт от git** — всё в этой папке, кроме этого файла;
- **никуда не копируется и не отправляется**;
- **промежуточный:** из него `engine/sync.py` переносит нужное в `чаты/data/`.
  Для работы нужен последний снимок; что делать со старыми — решает Айгуль.
