# Решения о методе

Почему конструктор устроен так. По файлу, датировано: `YYYY-MM-DD-имя.md`.

Сюда — только про метод: форма карточки, порядок сборки, что признано вредным.
Решения по конкретной построенной базе живут в её собственном `vault/decisions/`.
