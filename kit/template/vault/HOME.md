# <База> — карта

Отсюда начинается навигация. Как устроен namespace и как действовать — в `CLAUDE.md`.
Состояние на сегодня — [[status]]. Читай его в начале сессии.

## Зоны

- **[[<зона>|<Зона>]]** — <что там>. Данные лежат в `<зона>/`, правила зоны —
  файл `<зона>.md` в `vault/systems/` (ссылка выше ведёт в него по имени).

## Как действовать

- **Операции** — `vault/operations/`. На запись: <…>. На ответ: <…>.
- [[sources|Источники]] — откуда пришли данные и чего в них нет.
- [[questions-ВЛАДЕЛЕЦ|Вопросы владельцу]] — что не сошлось. Разбирать по одному.
