---
тип: состояние
на дату: 2026-09-14
---

# Как сейчас

Снимок на **2026-09-14**, день заведения базы. Данных ещё нет.

## Что в базе есть

- **Знание о деле** — из её шаблонов от 29.06.2026: [[услуги-и-цены]] ·
  [[анкета]] · [[шаблоны-сообщений]].
- **Домен `чаты/`** — что хранится, откуда и где врёт: [[зона-данных]]; восемь
  операций, все `in-dev` ([[operations]]).
- **Скрипты WhatsApp** — `engine/probe.py` (цифры) и `engine/snapshot.py` (копия
  в `transit/`). Проверены на искусственной базе; на настоящем WhatsApp ещё не
  запускались.
- **Временный публичный репозиторий** `github.com/skandar11/meeting` — знание и
  код; `чаты/data/` и `transit/` в него не входят ([[2026-09-14-baza-do-razgovora]]).

## Чего нет

| Чего нет | Когда появится |
|---|---|
| Людей, анкет, оплат, состава групп | второй визит |
| Формы `чаты/data/` — файлы или SQLite | первый визит, по цифрам probe |
| `engine/sync.py` — перенос из снимка в данные | между визитами |
| Ответов Айгуль — [[questions-АЙГУЛЬ]] | первый визит |
| Доменов `сопровождение/` и `вечера/` | после разговора |

## Сборка — два визита

### Визит 1 — 15.09.2026

1. **Command Line Tools** (git и python3) — запустить первым, ставится фоном.
   Подробно — `engine/setup.md`.
2. **Claude** — на её аккаунте.
3. **WhatsApp для Mac** — обычный, привязан к её телефону, открыт; дать ему
   подтянуть историю.
4. **База на её Маке** — после Command Line Tools:
   `git clone https://github.com/skandar11/meeting.git`.
5. **`python3 engine/probe.py`** и **`python3 engine/probe.py --groups`** — цифры
   записать в [[sources]] §2 с датой.
6. **Разговор** по [[questions-АЙГУЛЬ]], вопросы 1–6 — ответы туда же.
7. **Форма данных** — выбрать по цифрам probe, записать в [[зона-данных]].
8. **`python3 engine/snapshot.py`** — первый снимок в `transit/`.

### Между визитами

Разрез доменов и операции — по её ответам. `engine/sync.py` — под выбранную
форму данных и под то, как люди на самом деле заполняют анкету. Её переписка и
данные с её Мака не уходят.

### Визит 2 — дата не назначена

1. Обновление базы на её Маке — `git pull`, пока репозиторий жив; удалён — git
   bundle.
2. Свежий снимок → `engine/sync.py` → данные в `чаты/data/`.
3. Три-четыре её вопроса из разговора — через базу. Не отвечает — база не готова.
4. Показать Айгуль, как спрашивать базу своими словами.

## Что мешает

Разговора не было: домен и операции на ответ — наши догадки по её шаблонам
([[2026-09-14-baza-do-razgovora]]).
